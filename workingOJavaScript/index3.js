function sumNumbers(a, b) {
    // Declaring a variable inside the function before assigning a value
    var result;
    result = a + b;
    return result;
  }
  
  console.log(sumNumbers(8, 9)); // Outputs: 17
  